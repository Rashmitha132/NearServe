{
  "name": "nearserve-frontend",
  "version": "1.0.0",
  "description": "NearServe static frontend for Firebase Hosting",
  "private": true,
  "scripts": {
    "serve": "firebase emulators:start --only hosting",
    "deploy": "firebase deploy --only hosting"
  },
  "devDependencies": {}
}
